SECURE API

Make sure the site on production is configure to the most restict certificate.
create a user section managage to hold user info and login token.
Do not save any role information or access lavel information in the client computer, allways on the service.
create a role base function lavel access control to all api.
Add all the protections needed at CONTENT Header.
Add filter to sanitazed request and response.
Add protection to cookies is needed like secure cookie = extrict etc on program.cs
Allway encode with the most powerfull encode method all PII "personal information identityfier".
Implement a robust logging system to log all failers on the system special fail login attemp:
     Ensure all Failures are properly logged with suficient user context.
     Log entries Should be held for a sufficient time to allow  thorough forensic analysis.
     Ensure that logs are generated in a format that can be easily consume by a centralized log managent solution.
     Ensure hight value transactions have an audit trail with integrity controls to prevent tampering or deletion such as append-only database table or similar.
     and finally, make sure you have affective monitoring and alerting so that suspicious activities are detected and hendled in a timely fashion.
     NOTE: Make sure to use filters for any acros the board action like sanitazed, error login etc..
Add implementation to lock accounts.
Insuficient Anti-Automation:




